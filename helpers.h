#ifndef HELPERS_H
# define HELPERS_H
# include <stdlib.h>
# include <limits.h>

size_t				strlen_safe(const char *s);
char				*ft_itoa(int n);
char				*ft_strcpy(char *dst, char *src);
char				*ft_itohex(unsigned int n);

#endif
